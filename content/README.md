# Jekyll コンテンツ (/content)

ローカルプレビュー:

1. 必要: Ruby と Bundler
2. 初回のみ: `bundle install`
3. 起動: `bundle exec jekyll serve`

ブログは http://127.0.0.1:4000 で確認できます。

API (JSON): `/api/posts.json` に記事一覧が JSON で出力されます。

